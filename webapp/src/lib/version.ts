// Static prototype version string, shown in the account dropdown — mirrors
// the extension's "V. 1.7.21" in its own account menu. No real release
// pipeline stamps this in the webapp yet, so it's hand-maintained.
export const APP_VERSION = "2.0.14"
