# Local-only files at the repo root
.DS_Store
.idea/
*.log

# Editor files (project-wide)
.vscode/
*.swp

# Isolated git worktrees created by background agent tooling (each is a full
# checkout on its own worktree-agent-* branch, not project source)
.claude/worktrees/

# Anything specific to the cms/ subproject is handled by cms/.gitignore
