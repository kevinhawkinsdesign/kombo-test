{
  "mcpServers": {
    "payload": {
      "type": "http",
      "url": "${PAYLOAD_MCP_URL:-http://127.0.0.1:3000/api/mcp}",
      "headers": {
        "Authorization": "Bearer ${PAYLOAD_MCP_API_KEY}"
      }
    }
  }
}
